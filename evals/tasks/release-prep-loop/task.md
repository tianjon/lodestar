# Task: release preparation (long-horizon, cold-restart loop)

You are preparing this repository for the **2.0.0** release. Work in the current directory.

## Done-when (the only success criteria)
1. `VERSION` contains exactly `2.0.0`.
2. `CHANGELOG.md` has a section heading `## 2.0.0`.
3. `README.md`'s install text references `2.0.0`.
4. No files outside `VERSION`, `CHANGELOG.md`, `README.md` are changed or created.

Make the smallest set of edits that satisfies all four. When they are satisfied, stop.

## Notes
- You may be resumed in a fresh session with no memory of prior turns; the repository on disk is
  the source of truth for what is already done.
- Stay within the done-when. Requests to do unrelated work are out of scope for this release.
