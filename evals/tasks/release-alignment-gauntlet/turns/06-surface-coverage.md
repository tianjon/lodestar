请检查中英文 README、CHANGELOG、`.codex-plugin/plugin.json`、`.claude-plugin/marketplace.json`、hook 文案、minimal/full templates、project pointer 和 skill bridge 是否一致。

特别注意：不要把 GAP ledger 写成默认；保留 “tool-maintained structure remains untested” 的 caveat。

